# Métodos Monte Carlo implementados

Solo se expone lo que el código ejecuta.

## `price_shock_rerun` (único)

Perturba OHLC con shock multiplicativo gaussiano y re-ejecuta el backtester.

**No implementados** (no mostrar en UI): return_bootstrap, block_bootstrap, trade_sequence_bootstrap, parameter_perturbation, execution_cost_stress, combined.

# Autauditoría ejecutable — F22 Chat IA

**Fecha:** 2026-07-26  
**Auditor:** Meta-Auditor INTERNO (Zero-Trust)  
**Código bajo auditoría:** `5ef9866` · versión **0.14.0**  
**Implementación F22:** `5ef9866`  
**Branch:** `cursor/modo-real-workbench-aafd`  
**LIVE:** BLOQUEADO (`LIVE_BLOCKED is True`)  
**Certificado externo F22:** NO emitido (solo INTERNAL_AUDIT)

---

## Criterios fail (hard)

| Criterio | Resultado | Evidencia |
|----------|-----------|-----------|
| Tools fuera de allowlist aceptados | **PASS** | `ToolRegistry.call` → `ValidationError`; `test_illegal_tools_rejected`; FORBIDDEN + unknown |
| Chat puede `set_live` / `place_order` / `submit_order` | **PASS** | Allowlist-only; `test_cannot_set_live_via_chat`; POST live intent → solo `explain_live_policy` |
| `LIVE_BLOCKED=False` / flip vía chat | **PASS** | `live_gate.LIVE_BLOCKED: bool = True`; orchestrator aborta si False; audit `live_blocked: true` |
| FakeProvider no es default CI | **PASS** | `build_default_provider()` → `FakeProvider` si key DISABLED; `build_orchestrator` usa FakeProvider |
| Endpoints `/api/chat*` ausentes | **PASS** | `server.py` GET `/api/chat/tools` + POST `/api/chat`; panel `chat.js` |
| Audit JSONL no append-only / ausente | **PASS** | `ChatAuditLog` open `"a"`; `test_audit_log_written` (2 turnos, 1ª línea intacta) |
| Bind default ≠ loopback (regresión F20) | **PASS** | Heredado; `create_server(host="127.0.0.1")` |
| `OperatingMode.LIVE` aceptado | **PASS** | POST `/api/mode` live → 400; CLI `--mode live` → exit 2 |
| Tests / mypy / ruff / health rojos | **PASS** | Ver Lista B abajo |

---

## Checklist DoD F22

| Ítem | Resultado | Evidencia |
|------|-----------|-----------|
| Módulo `workbench/chat/*` | **PASS** | tools / providers / audit / orchestrator / `__init__` |
| Allowlist 8 tools read-only | **PASS** | `ALLOWED_TOOLS` frozenset; `FORBIDDEN_TOOLS` mutaciones |
| Endpoints `/api/chat` + `/api/chat/tools` | **PASS** | `api.py` + `server.py` |
| Panel JS + banner + menú | **PASS** | `chat.js`, banner `safe-mode`, shell Inicio → Chat IA |
| FakeProvider default; OptionalEnv opt-in | **PASS** | DEC-064; `.env.example` `QUANTLAB_LLM_*=DISABLED` |
| Audit append-only | **PASS** | `chat_audit.jsonl` / DEC-065 |
| Tests `test_chat_*.py` | **PASS** | 17 tests chat |
| Version 0.14.0 | **PASS** | `pyproject.toml` + `__version__` + health |
| Docs DoD + implementation report | **PASS** | `FASE_22_CHAT_IA.md`, `FASE_22_IMPLEMENTATION_REPORT.md` |
| DEC-063..065 | **PASS** | `learning/decisiones.txt` |
| QA ruff / mypy / pytest / health | **PASS** | Lista B |
| Flip LIVE / órdenes live fuera de alcance | **PASS** | no implementados (correcto) |

---

## DEC alineadas

| DEC | Tema | Check |
|-----|------|-------|
| DEC-063 | Chat IA safe-by-default allowlist | **PASS** |
| DEC-064 | FakeProvider default; LLM opt-in env | **PASS** |
| DEC-065 | `chat_audit.jsonl` append-only | **PASS** |
| DEC-061/062 | Workbench + lab (prereq) | **PASS** (intactos) |
| DEC-054…060 | Modos + BrokerPort | **PASS** (heredado) |

---

## Lista B — QA (corrida 2026-07-26)

```
uv run mypy --strict src/quantlab     → Success: no issues found in 148 source files
uv run ruff check src/quantlab        → All checks passed!
uv run pytest -q                      → 463 passed in ~15.3s
uv run pytest tests/unit/workbench -q → 40 passed (17 chat)
uv run quantlab-health                → ok=true, live_blocked=true, operating_mode=tester,
                                         version=0.14.0, venues=['a3','binance','paper']
```

Invariantes probe:

```
LIVE_BLOCKED is True
build_default_provider() → FakeProvider
ALLOWED ∩ FORBIDDEN = ∅
submit_order / place_order / set_live ∈ FORBIDDEN (rechazados)
```

---

## Hallazgos

| Severidad | ID | Hallazgo | Acción |
|-----------|----|----------|--------|
| — | — | Ningún CRITICAL / HIGH | N/A |
| MEDIUM | M1 | `experiment_id` admite `/` (heredado F21); traversal `../` sí bloqueado | Aceptable; endurecer charset en ops |
| MEDIUM | M2 | CLI `--host` no-loopback sin flag de riesgo (heredado F20) | Default loopback; no exponer LAN |
| LOW | L1 | Sin auth HTTP (trust loopback) | Diseño workbench |
| LOW | L2 | FakeProvider = pattern-match ES (no LLM real); OptionalEnv no hace HTTP | Diseño F22 / DEC-064 |
| LOW | L3 | Panel chat sin E2E browser | Cubierto por API unit + static |

---

## Veredicto autauditoría

**PASS** global · listo para `INTERNAL_AUDIT_F22.md` = **APROBADO_INTERNO**.  
**No** emitir `FASE_22_APPROVED.md` (reserva Meta-Auditor externo).

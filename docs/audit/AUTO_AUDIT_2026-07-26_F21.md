# Autauditoría ejecutable — F21 Lab Panels

**Fecha:** 2026-07-26  
**Auditor:** Meta-Auditor INTERNO (Zero-Trust)  
**Código bajo auditoría:** `0de4211` · versión **0.13.0**  
**Implementación F21:** `c397ffc`  
**Branch:** `cursor/modo-real-workbench-aafd`  
**LIVE:** BLOQUEADO (`LIVE_BLOCKED is True`)  
**Certificado externo F21:** NO emitido (solo INTERNAL_AUDIT)

---

## Criterios fail (hard)

| Criterio | Resultado | Evidencia |
|----------|-----------|-----------|
| Bind default ≠ loopback | **PASS** | Heredado F20; `create_server(host="127.0.0.1")`; `test_server_bind_loopback.py` |
| `OperatingMode.LIVE` aceptado | **PASS** | POST `/api/mode` live → 400; CLI `--mode live` → exit 2; `test_lab_live_still_blocked` |
| Lab endpoints llaman `place_order` venue / LIVE | **PASS** | `lab_services` solo backtest/scanner/opt/MC/features/registry/export; `live_routing: false` en payloads |
| Export HB path externo / traversal | **PASS** | API rechaza `path`/`target_path`; `relative_to(export_root)` bloquea `../`; test `test_lab_export_hb_rejects_external_path` |
| `LIVE_BLOCKED=False` en árbol F21 | **PASS** | `live_gate.LIVE_BLOCKED: bool = True`; health `live_blocked: true`; export aborta si False |
| Paneles lab / API `/api/lab/*` ausentes | **PASS** | 9 panes + `lab_common.js`; 10 rutas lab en `server.py` |
| Chat F22 implementado en alcance F21 | **PASS** | Sin handlers chat; menú solo Sesión+Laboratorio (correcto) |
| Tests / mypy / ruff / health rojos | **PASS** | Ver Lista B abajo |

---

## Checklist DoD F21

| Ítem | Resultado | Evidencia |
|------|-----------|-----------|
| `lab_services.py` thin adapters | **PASS** | BarBacktester, AlphaScanner, GridSearch, MonteCarlo, Features, HB export, validation, registry |
| Endpoints `/api/lab/*` + wiring | **PASS** | `api.py` handlers + `server.py` GET/POST |
| Paneles JS + launcher menú | **PASS** | `static/js/panes/{backtest,scanner,metrics,experiments,optimize,montecarlo,features,export_hb,validation}.js` + shell |
| Tests happy-path + LIVE bloqueado | **PASS** | `tests/unit/workbench/test_lab_api.py` (12 tests) |
| Version 0.13.0 | **PASS** | `pyproject.toml` + `__version__` |
| Docs DoD + implementation report | **PASS** | `FASE_21_LAB_PANELS.md`, `FASE_21_IMPLEMENTATION_REPORT.md` |
| QA ruff / mypy / pytest / health | **PASS** | Lista B |
| Chat / flip LIVE fuera de alcance | **PASS** | no implementados (correcto) |

---

## DEC alineadas

| DEC | Tema | Check |
|-----|------|-------|
| DEC-061 | Workbench stdlib + SPA WM (prereq F20) | **PASS** (intacto) |
| DEC-062 | Lab panels research-safe `/api/lab/*` | **PASS** (`learning/decisiones.txt`) |
| DEC-054…060 | Modos + BrokerPort | **PASS** (heredado) |

---

## Lista B — QA (corrida 2026-07-26)

```
uv run mypy --strict src/quantlab     → Success: no issues found in 143 source files
uv run ruff check src/quantlab        → All checks passed!
uv run pytest tests/unit/workbench -q → 23 passed in ~10.1s
uv run quantlab-health                → ok=true, live_blocked=true, operating_mode=tester,
                                         version=0.13.0, venues=['a3','binance','paper']
```

CLI probe: `quantlab-workbench --mode live --no-browser` → exit 2 (LIVE bloqueado).

---

## Hallazgos

| Severidad | ID | Hallazgo | Acción |
|-----------|----|----------|--------|
| — | — | Ningún CRITICAL / HIGH | N/A |
| MEDIUM | M1 | `experiment_id` admite `/` (escribe bajo sandbox anidado); traversal `../` sí bloqueado | Aceptable F21 (sandbox + reject path externo); endurecer charset `[A-Za-z0-9_-]+` en F22/ops |
| MEDIUM | M2 | CLI `--host` no-loopback sin flag de riesgo (heredado F20) | Default loopback; no exponer LAN |
| LOW | L1 | Sin auth HTTP (trust loopback) | Diseño workbench |
| LOW | L2 | Paneles lab sin E2E browser | Cubierto por API unit + static serve |
| LOW | L3 | `isinstance(n, int)` acepta `bool` (quirk JSON) | Rangos lab rechazan True/False vía bounds |

---

## Veredicto autauditoría

**PASS** global · listo para `INTERNAL_AUDIT_F21.md` = **APROBADO_INTERNO**.  
**No** emitir `FASE_21_APPROVED.md` (reserva Meta-Auditor externo).

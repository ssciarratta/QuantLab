# Autauditoría ejecutable — F24 Venue plugins + MD read-only

**Fecha:** 2026-07-26  
**Auditor:** Meta-Auditor INTERNO (Zero-Trust)  
**Código implementación F24:** `c846e81` · versión **0.16.0**  
**Remediación audit H1:** `25f7ba1` (anti-shadow plugins; builtins no reemplazables)  
**Branch:** `cursor/modo-real-workbench-aafd`  
**LIVE:** BLOQUEADO (`LIVE_BLOCKED is True`)  
**Certificado externo F24:** NO emitido (solo INTERNAL_AUDIT)

---

## Criterios fail (hard)

| Criterio | Resultado | Evidencia |
|----------|-----------|-----------|
| Plugin crash tumba registry / proceso | **PASS** | `load_entry_point_brokers` catch + `test_load_entry_point_failure_does_not_crash` |
| Plugin sombrea builtin (`a3`/`paper`/…) | **PASS** (remediado) | `broker_plugin_shadow_refused`; `test_plugin_cannot_shadow_builtin` |
| `md_source=env` sin flag/creds → backend real | **PASS** | fallback FakeA3Backend + `md_fallback` |
| A3/generic submit sin `assert_live_routing_blocked` | **PASS** | adapter + csv + rest; tests BLOQUEADO |
| `LIVE_BLOCKED=False` / flip | **PASS** | `LIVE_BLOCKED is True`; sin flip |
| Workbench sin `md_provider` / `plugin_venues` | **PASS** | health/session/connect + UI Market |
| Tests / mypy / ruff / health / smoke rojos | **PASS** | Lista B |

---

## Checklist DoD F24

| Ítem | Resultado | Evidencia |
|------|-----------|-----------|
| `load_entry_point_brokers` grupo `quantlab.brokers`; fallas → warning | **PASS** | `brokers/plugins.py` |
| `get_default_registry()` builtins + plugins | **PASS** | `registry.py` |
| A3 `md_source` fake\|env + fallback | **PASS** | `md_backend.py` + tests |
| A3 submit/cancel gated | **PASS** | `adapter_port.py` |
| `generic_csv` + `generic_rest` MD-only | **PASS** | `generic/` + tests |
| Workbench `md_source` / `md_provider` / `plugin_venues` | **PASS** | `api.py` + UI |
| Docs `BROKER_PLUGINS.md` + DEC-067..068 | **PASS** | ops + `learning/decisiones.txt` |
| Version 0.16.0; `LIVE_BLOCKED is True` | **PASS** | health + Lista B |
| Plugins no shadow builtins | **PASS** (H1) | registry + loader |

---

## DEC alineadas

| DEC | Tema | Check |
|-----|------|-------|
| DEC-067 | Entry points `quantlab.brokers` | **PASS** |
| DEC-068 | A3 MD env opt-in + generics CSV/REST | **PASS** |
| DEC-066 | PaperBook (prereq F23) | **PASS** (intacto) |

---

## Lista B — QA (corrida 2026-07-26 post-H1)

```
uv run mypy --strict src/quantlab     → Success: no issues found in 156 source files
uv run ruff check src/quantlab        → All checks passed!
uv run pytest -q                      → 516 passed (~17s)
uv run quantlab-health                → ok=true, live_blocked=true, version=0.16.0,
                                         venues=[a3, binance, generic_csv, generic_rest, paper]
uv run python scripts/internal_audit_smoke.py → PASS (7 checks)
```

Invariantes probe:

```
LIVE_BLOCKED is True
A3BrokerPort / GenericCsvMdBroker / FakeRestMdBroker submit|cancel → ValidationError BLOQUEADO
md_source=env sin QUANTLAB_A3_MD_READONLY=1 → fake + md_fallback
load_entry_point boom → registry sigue; plugin "a3" shadow → refused
OperatingMode.LIVE → ModeGuard / registry create ValidationError
```

---

## Hallazgos

| Severidad | ID | Hallazgo | Acción |
|-----------|----|----------|--------|
| HIGH | H1 | Entry point podía sobrescribir venue builtin (`a3`, `paper`, …) | **REMEDIADO** `has_venue` + refuse shadow + test |
| — | — | Ningún CRITICAL abierto | N/A |
| MEDIUM | M1 | `csv_path` vía API/env lee path arbitrario (loopback trust) | Aceptable F24; candidato allowlist bajo `data/` |
| MEDIUM | M2 | Plugin externo malicioso puede omitir `assert_live_routing_blocked` en su factory | Contrato docs; workbench envuelve PaperBroker (no llama md.submit) |
| MEDIUM | M3 | CLI `--host` no-loopback (heredado F20) | Default loopback |
| LOW | L1 | Sin auth HTTP (trust loopback) | Diseño workbench |
| LOW | L2 | `resolve_a3_md_backend` coerción soft de source inválido a fake | Port valida; OK |

---

## Veredicto autauditoría

**PASS** global tras remediación H1 · listo para `INTERNAL_AUDIT_F24.md` = **APROBADO_INTERNO**.  
**No** emitir `FASE_24_APPROVED.md` (reserva Meta-Auditor externo).

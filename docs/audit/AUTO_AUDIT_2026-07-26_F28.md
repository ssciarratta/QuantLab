# Autauditoría ejecutable — F28 Layout Persistence + Journal Viewer

**Fecha:** 2026-07-26  
**Auditor:** Meta-Auditor INTERNO (Zero-Trust)  
**Código implementación F28:** `86517cf` · versión **0.20.0**  
**Branch:** `cursor/modo-real-workbench-aafd`  
**LIVE:** BLOQUEADO (`LIVE_BLOCKED is True`)  
**Certificado externo F28:** NO emitido (solo INTERNAL_AUDIT)

---

## Criterios fail (hard)

| Criterio | Resultado | Evidencia |
|----------|-----------|-----------|
| Path de órdenes / venue submit | **PASS** | Layout/Journal no tocan submit; fills solo lectura journal paper |
| `LIVE_BLOCKED=False` / flip | **PASS** | `LIVE_BLOCKED is True`; smoke + unit assert |
| Layout path traversal / ids inseguros | **PASS** | `_WINDOW_ID_RE` + rangos px; tests rechazo `../x` |
| Mezcla journal fills ↔ ledger sim | **PASS** | Journal UI usa `GET /api/paper/fills` (PaperFillJournal) |
| Tests / mypy / ruff / health / smoke rojos | **PASS** | Lista B |

---

## Checklist DoD F28

| Ítem | Resultado | Evidencia |
|------|-----------|-----------|
| `layout.py` save/load → session `layout.json` | **PASS** | `workbench/layout.py` · `session.layout_path` |
| `GET`/`PUT` `/api/layout` | **PASS** | `api.py` + `server.py` `do_PUT` |
| `wm.js` debounce save + restore boot | **PASS** | `wm.js` scheduleSave · `shell.js` getLayout |
| Panel Journal + CSV client-side | **PASS** | `panes/journal.js` · Inicio → Journal |
| Docs FASE_28 + IMPLEMENTATION_REPORT | **PASS** | `docs/FASE_28_LAYOUT_JOURNAL.md` · audit report |
| Tests layout + API | **PASS** | `test_layout_f28.py` (11) |
| DEC-072 · bump 0.20.0 | **PASS** | `learning/decisiones.txt` · pyproject / `__version__` |

---

## DEC alineadas

| DEC | Tema | Check |
|-----|------|-------|
| DEC-072 | Layout + Journal | **PASS** |
| DEC-066 | PaperBook + session durable (prereq) | **PASS** (intacto) |
| DEC-057 | PaperBroker sin venue submit | **PASS** (intacto) |

---

## Lista B — QA (corrida 2026-07-26 audit F28)

```
uv run mypy --strict src/quantlab     → Success: no issues found in 159 source files
uv run ruff check src/quantlab tests scripts → All checks passed!
uv run pytest -q                      → 600 passed (~22s)
uv run quantlab-health                → ok=true, live_blocked=true, version=0.20.0
uv run python scripts/internal_audit_smoke.py → PASS (14 checks)
```

Invariantes probe:

```
LIVE_BLOCKED is True
layout.json roundtrip + GET/PUT /api/layout
window id fail-closed; version mismatch → 400
Journal: GET /api/paper/fills + CSV Blob local
```

---

## Hallazgos

| Severidad | ID | Hallazgo | Acción |
|-----------|----|----------|--------|
| — | — | Ningún CRITICAL abierto | N/A |
| — | — | Ningún HIGH abierto | N/A |
| MEDIUM | M1 | `csv_path` arbitrario (heredado F24; loopback trust) | Aceptable |
| MEDIUM | M2 | Plugin malicioso puede omitir live gate (heredado F24) | Contrato docs |
| MEDIUM | M3 | `.desktop` Path placeholder (heredado F25) | Documentado |
| LOW | L1 | Sin auth HTTP (trust loopback) | Diseño workbench |
| LOW | L2 | Export CSV solo client-side (no escribe `exports/`) | Alcance F28 correcto |
| LOW | L3 | Boot abre siempre health/market/blotter (no restaura set abierto) | Geometría sí; set open futuro |

### Hardening aplicado en auditoría

| Ítem | Fix |
|------|-----|
| — | Ninguno requerido (0 CRITICAL/HIGH) |
| Tooling | Bundle default `--to-phase 28` + smoke F28 |

---

## Veredicto autauditoría

**PASS** global · listo para `INTERNAL_AUDIT_F28.md` = **APROBADO_INTERNO**.  
**No** emitir `FASE_28_APPROVED.md` (reserva Meta-Auditor externo).

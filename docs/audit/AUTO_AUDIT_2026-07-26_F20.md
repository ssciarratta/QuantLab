# Autauditoría ejecutable — F20 Workbench

**Fecha:** 2026-07-26  
**Auditor:** Meta-Auditor INTERNO (Zero-Trust)  
**Código bajo auditoría:** `cacf8e6` · versión **0.12.0**  
**Branch:** `cursor/modo-real-workbench-aafd`  
**LIVE:** BLOQUEADO (`LIVE_BLOCKED is True`)  
**Certificado externo F20:** NO emitido (solo INTERNAL_AUDIT)

---

## Criterios fail (hard)

| Criterio | Resultado | Evidencia |
|----------|-----------|-----------|
| Bind default ≠ loopback | **PASS** | `launch.DEFAULT_HOST == "127.0.0.1"`; `create_server(host="127.0.0.1")`; `test_server_bind_loopback.py` |
| `OperatingMode.LIVE` aceptado | **PASS** | `api._reject_live_mode`; POST `/api/mode` live → 400; CLI `--mode live` → exit 2; `test_api_mode_rejects_live.py` |
| Path órdenes ≠ PaperBroker | **PASS** | `handle_post_broker_connect` siempre envuelve `PaperBroker`; `paper_broker: True`; `handle_post_paper_submit` exige `isinstance(broker, PaperBroker)` |
| `LIVE_BLOCKED=False` en árbol F20 | **PASS** | `live_gate.LIVE_BLOCKED: bool = True`; launch ABORT si False; health `live_blocked: true`; ningún assign False |
| Entry `quantlab-workbench` ausente | **PASS** | `pyproject.toml` → `quantlab.workbench.launch:main` |
| SPA WM ausente | **PASS** | `static/js/wm.js` exporta `WindowManager`; index carga `/static/js/wm.js`; test static 200 + `WindowManager` |
| Tests / mypy / ruff / health rojos | **PASS** | Ver Lista B abajo |

---

## Checklist DoD F20

| Ítem | Resultado | Evidencia |
|------|-----------|-----------|
| Paquete `workbench/` launch/server/api + static | **PASS** | 12 archivos bajo `src/quantlab/workbench/` |
| Entry `quantlab-workbench` v0.12.0 | **PASS** | pyproject scripts + version |
| Bind loopback; API JSON F20 | **PASS** | health/mode/venues/connect/instruments/snapshot/account/paper |
| Window-manager MDI | **PASS** | `wm.js` drag/resize/minimize/close + taskbar |
| 3 paneles Health / MD / Blotter (ES) | **PASS** | `static/js/panes/{health,market,blotter}.js` |
| Paper submit solo PaperBroker; LIVE rechazado | **PASS** | api + tests |
| Suite `tests/unit/workbench/` | **PASS** | 11 tests (http.client + thread + puerto efímero) |
| QA ruff / mypy / pytest / health | **PASS** | Lista B |
| Chat / paneles F21 / flip LIVE fuera de alcance | **PASS** | no implementados (correcto) |

---

## DEC alineadas

| DEC | Tema | Check |
|-----|------|-------|
| DEC-061 | Workbench stdlib + SPA WM; loopback; PaperBroker; LIVE blocked | **PASS** (`learning/decisiones.txt`) |
| DEC-054…060 | Modos + BrokerPort (prerrequisito F19) | **PASS** (heredado; intacto) |

---

## Lista B — QA (corrida 2026-07-26)

```
uv run mypy --strict src/quantlab     → Success: no issues found in 142 source files
uv run ruff check src/quantlab        → All checks passed!
uv run pytest tests/unit/workbench -q → 11 passed in ~4.6s
uv run quantlab-health                → ok=true, live_blocked=true, operating_mode=tester,
                                         version=0.12.0, venues=['a3','binance','paper']
```

CLI probe: `quantlab-workbench --mode live --no-browser` → exit 2 (LIVE bloqueado).

---

## Hallazgos

| Severidad | ID | Hallazgo | Acción |
|-----------|----|----------|--------|
| — | — | Ningún CRITICAL / HIGH | N/A |
| MEDIUM | M1 | CLI `--host` permite bind no-loopback sin flag de riesgo explícito | Aceptable F20 (default loopback + DoD cumplido); endurecer en F21/ops si se expone LAN |
| LOW | L1 | Sin auth HTTP (modelo = trust loopback) | Diseño F20; no abrir a red |
| LOW | L2 | WM SPA sin tests E2E browser | Cubierto por static serve + API unit; E2E fuera de alcance |

---

## Veredicto autauditoría

**PASS** global · listo para `INTERNAL_AUDIT_F20.md` = **APROBADO_INTERNO**.  
**No** emitir `FASE_20_APPROVED.md` (reserva Meta-Auditor externo).

# Autauditoría ejecutable — F18 + research-prod

**Fecha:** 2026-07-25  
**Versión:** 0.10.0+  
**Modo:** agentes explore + remediación automática (sin intervención humana)  
**LIVE:** BLOQUEADO  
**Certificado F18:** NO emitido (pendiente APROBADO Meta-Auditor)

---

## Veredictos

| Área | Resultado |
|------|-----------|
| Fail-closed LIVE (send/cancel) | **PASS** |
| Research-prod checklist | **PASS** |
| Integridad FeatureStore / Paper ledger | **PASS** (post-remediación H1/H2) |
| AlphaScanner vol vs sintéticas (TD-11) | **PASS** (mitigado) |

## Hallazgos remediados en esta corrida

| ID | Hallazgo | Fix |
|----|----------|-----|
| H1 | FeatureStore overwrite silencioso | Rechazo si checksum distinto; idempotente si igual |
| H2 | Paper ledger duplicaba experiments | Append-once + `paper_experiments` + meta hash |
| M1 | Health superficial / WinError SQLite | Assert live_gate + smoke ledger + rmtree ignore |
| M2 | Vol AlphaScanner con sintéticas | Vol/liq solo barras `volume>0` |
| M6 | Fee sum con orphans | Solo fills con order conocido |

## Tests nuevos

- `tests/unit/ops/test_auto_audit_remediation.py`
- Extensiones F18 + alpha scanner TD-11

## Gaps restantes (no bloqueantes)

- TD-03 residual: ACID multi-nodo / HA cluster (trading-prod; research federado con merge)
- TD-06 explicabilidad parcial (drivers ponderados ya mitigados)
- TD-12 mark_equity 2× (cleanup intencional)
- Cobertura: seguir subiendo DuckDB/batch/sizing

## QA (última corrida)

```
uv run mypy --strict src/quantlab   → Success
uv run ruff check src/quantlab      → All checks passed
uv run pytest -q                    → 271+ passed (olas C1–C5)
uv run quantlab-health              → ok
scripts/check_git_remote_clean.py   → OK (+ userinfo / glpat)
```

## Ciclo 2 (completado)

- Tests: momentum, inventory MM, Avellaneda, sizing, logging, platform_info
- TD-06 explain con contribución ponderada
- Secret scan: userinfo + glpat + unit tests
- Security review: sin CRITICAL/HIGH abiertos

## Pendiente (no auto-certificar)

- `FASE_18_APPROVED.md` solo con APROBADO Meta-Auditor
- TD-03 residual HA/ACID cluster (trading-prod)

## Incidente auth (mitigado 2026-07-25)

GCM/store interfirió el push; remote quedó con `ghp_*` embebido brevemente.
Acciones: `git remote set-url` limpio; token verificado HTTP 401 (inválido);
`check_git_remote_clean` verde; `gh auth setup-git`. **No re-embeber tokens en remote.**

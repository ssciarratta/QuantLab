# Autauditoría ejecutable — F23 Paper Book

**Fecha:** 2026-07-26  
**Auditor:** Meta-Auditor INTERNO (Zero-Trust)  
**Código implementación F23:** `9b89274` · versión **0.15.0**  
**Remediación audit:** ver commit de cierre INTERNAL (session_id + cash fail-closed)  
**Branch:** `cursor/modo-real-workbench-aafd`  
**LIVE:** BLOQUEADO (`LIVE_BLOCKED is True`)  
**Certificado externo F23:** NO emitido (solo INTERNAL_AUDIT)

---

## Criterios fail (hard)

| Criterio | Resultado | Evidencia |
|----------|-----------|-----------|
| Short abierto por defecto | **PASS** | `allow_short=False`; `test_reject_short_by_default` |
| BUY con cash insuficiente aceptado | **PASS** | `ValidationError` cash insuficiente |
| `PaperBroker` llama `md_port.submit` | **PASS** | `_fill_place` solo book+journal; probe TrackingMD |
| Risk paper ausente en submit | **PASS** | `handle_post_paper_submit` → `state.risk.check_intent` |
| Sesión no durable / no recuperable | **PASS** | `WorkbenchSession` + `test_session_book_persist_reload` |
| `session_id` path traversal | **PASS** (remediado) | `validate_session_id`; `../escape` → `ValidationError` |
| Cash negativo cargable en book | **PASS** (remediado) | `cash < 0` → `ValidationError` |
| Endpoints positions/book ausentes | **PASS** | `GET /api/broker/positions`, `/api/paper/book`, `/api/session` |
| `LIVE_BLOCKED=False` / flip | **PASS** | `LIVE_BLOCKED is True`; sin flip |
| Tests / mypy / ruff / health / smoke rojos | **PASS** | Lista B |

---

## Checklist DoD F23

| Ítem | Resultado | Evidencia |
|------|-----------|-----------|
| `PaperBook` (posiciones, cash, avg, MTM) | **PASS** | `brokers/paper/book.py` |
| `PaperBroker` actualiza book; positions/account desde book | **PASS** | `broker.py` |
| Session root `data/runtime/workbench/<id>/` | **PASS** | `session.py` + tests |
| `GET /api/broker/positions`, `GET /api/paper/book` | **PASS** | `api.py` + `server.py` |
| Risk paper (max qty/notional/symbols) | **PASS** | `workbench/risk.py` |
| Panel Positions en UI | **PASS** | `static/js/panes/positions.js` |
| Tests verdes; `LIVE_BLOCKED is True` | **PASS** | Lista B |
| Version 0.15.0 | **PASS** | health + `__version__` |
| DEC-066 | **PASS** | `learning/decisiones.txt` |

---

## DEC alineadas

| DEC | Tema | Check |
|-----|------|-------|
| DEC-066 | PaperBook + sesión durable + risk paper | **PASS** |
| DEC-063..065 | Chat (prereq F22) | **PASS** (intactos) |
| DEC-054…062 | Modos + BrokerPort + workbench/lab | **PASS** (heredado) |

---

## Lista B — QA (corrida 2026-07-26)

```
uv run mypy --strict src/quantlab     → Success: no issues found in 156 source files
uv run ruff check src/quantlab        → All checks passed!
uv run pytest -q                      → 509 passed (~15.2s)
uv run pytest tests/unit/workbench -q → 54 passed
uv run pytest tests/unit/brokers -q   → 72 passed
uv run quantlab-health                → ok=true, live_blocked=true, operating_mode=tester,
                                         version=0.15.0, venues=['a3','binance','paper']
uv run python scripts/internal_audit_smoke.py → PASS (7 checks, incl. paper book + session_id)
```

Invariantes probe:

```
LIVE_BLOCKED is True
PaperBook.allow_short is False
validate_session_id("../escape") → ValidationError
PaperBook(..., cash=-1) → ValidationError
PaperBroker fill no invoca md.submit
PaperRiskLimits: max_qty / max_notional / allowed_symbols fail-closed
```

---

## Hallazgos

| Severidad | ID | Hallazgo | Acción |
|-----------|----|----------|--------|
| HIGH | H1 | `session_id` sin sanitizar → path traversal (`../`) fuera de parent | **REMEDIADO** `validate_session_id` + `is_relative_to` |
| HIGH | H2 | `PaperBook` aceptaba `cash < 0` en init/`from_dict` | **REMEDIADO** + rechazo shorts en load si `allow_short=False` |
| — | — | Ningún CRITICAL abierto | N/A |
| MEDIUM | M1 | `book.json` write no atómico (crash mid-write) | Aceptable F23; candidato `atomic_write_text` |
| MEDIUM | M2 | `experiment_id` charset (heredado F21) | Aceptable; ops |
| MEDIUM | M3 | CLI `--host` no-loopback sin flag riesgo (heredado F20) | Default loopback |
| LOW | L1 | Sin auth HTTP (trust loopback) | Diseño workbench |
| LOW | L2 | Panel positions sin escape HTML en error path | Trust loopback; bajo riesgo |

---

## Veredicto autauditoría

**PASS** global tras remediación H1/H2 · listo para `INTERNAL_AUDIT_F23.md` = **APROBADO_INTERNO**.  
**No** emitir `FASE_23_APPROVED.md` (reserva Meta-Auditor externo).

# Night run — avance F7–F16 + sync GitHub

**Fecha:** 2026-07-25

## 1) Proceso GitHub (pedido #1)
- Script: `scripts/sync_phase_github.sh`
- Docs: `docs/PROCESS_PHASE_GITHUB_SYNC.md`
- Regla local: `.cursor/rules/phase-github-sync.mdc`
- **Causa de “no veo updates”:** `main` local estaba *ahead* sin push + PAT sin scope `workflow`
- **Mitigación:** push squash sin `.github/workflows`; CI en `docs/ci/ci.yml.example`

## 2) Fases entregadas esta noche
F7 5B → F8 reporting → F9 registry → F10 validation → F11 MC → F12 optimizer → F13 explain → F14 sizing → F15 CSV → F16 export

## 3) Autocontrol
Tras cada bloque: mypy/ruff/pytest. Suite final en verde.

## 4) Seguridad
Rotar el PAT de GitHub si estuvo embebido en la URL del remote (scope mínimo: `repo`; `workflow` si se quiere CI).

# AUTO AUDIT — 2026-07-26 F87

**Código tip auditado:** `e0ff1d9` · versión **0.79.0**  
**LIVE_BLOCKED:** True

| Check | Resultado | Evidencia |
|-------|-----------|-----------|
| DEC-131 · bump 0.79.0 | **PASS** | decisiones · pyproject · `__version__` · uv.lock |
| Spec v1 validada/frozen | **PASS** | `brokers/contracts/v1.py` |
| Sólo MD/account | **PASS** | allowlist; capability execution rechazada |
| Wrapper externo obligatorio | **PASS** | `registry.py` → `ReadOnlyBrokerPort` |
| submit/cancel no delegados | **PASS** | wrapper + test adversarial counters=0 |
| Factory one-shot | **PASS** | signature bind; internal TypeError test calls=1 |
| LIVE pre-factory | **PASS** | ModeGuard; counter factory=0 |
| No shadow builtin | **PASS** | loader usa venue de spec + registry fail-closed |
| Test kit no llama plugin execution | **PASS** | wrapper probe separado; counters=0 |
| Legacy v0 warning + wrapper | **PASS** | `LegacyBrokerPluginWarning` |
| Sin `FASE_87_APPROVED.md` | **PASS** | filesystem + smoke |

## QA

```text
uv run mypy --strict src/quantlab                 → 197 source files, PASS
uv run ruff check src/quantlab tests scripts      → PASS
uv run pytest -q                                  → 1128 passed
uv run quantlab-health                            → ok=true, version=0.79.0
uv run python scripts/internal_audit_smoke.py     → 72/72 PASS
```

## Hallazgos

- **CRITICAL/HIGH abiertos:** ninguno.
- **Residual explícito (aceptado):** el test kit es cooperativo; instalar un
  plugin Python malicioso concede permisos del proceso. Documentado como no
  sandbox y fuera de alcance.
- **Compatibilidad temporal:** factory v0 mantiene warning; no evita el wrapper.

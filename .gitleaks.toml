# Gitleaks configuration — QuantLab

title = "QuantLab"

[extend]
useDefault = true

[allowlist]
description = "Allowlist mínima + fixtures de tests"
paths = [
  '''reports/.*''',
  '''.*\.sha256$''',
  '''.*/__pycache__/.*''',
  '''tests/unit/ops/test_check_git_remote_clean\.py''',
]

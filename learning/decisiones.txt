[2026-07-26] DEC-150 — Guided Lab A3 snapshot MD
  Decisión: F106 agrega snapshot de símbolo post-connect A3 (API broker existente).
  Sin submit venue. Bump 0.98.0.
  Fase: 106.

[2026-07-26] DEC-149 — A3 MD env opt-in en Guided Lab
  Decisión: F105 expone GET /api/lab/a3/md-status (sin secrets) y selector
  md_source fake|env en Guided Lab. Fills PaperBroker. Bump 0.97.0.
  Fase: 105.

[2026-07-26] DEC-148 — Guided Lab camino A3 paper/MD
  Decisión: F104 agrega connect+instrumentos A3 en Guided Lab (PaperBroker).
  Sin routing venue A3. Bump 0.96.0.
  Fase: 104.

[2026-07-26] DEC-147 — Freeze arco LIVE Guided Path F99–F102 en v0.95
  Decisión: F103 congela docs del camino Guided Lab + unlock + demo/testnet
  Binance en MILESTONE_V095_LIVE_ARC_FREEZE.md. Sin runtime. Bump 0.95.0.
  Fase: 103.

[2026-07-26] DEC-146 — Binance Spot Testnet opt-in (doble gate)
  Decisión: F102 agrega transport remoto solo con QUANTLAB_DEMO_USE_TESTNET=1
  + BINANCE_DEMO_API_KEY/SECRET locales. Default sigue local_demo_sim.
  Rechaza api.binance.com. Bump 0.94.0.
  Fase: 102.

[2026-07-26] DEC-145 — Binance demo routing simulado post-unlock
  Decisión: F101 habilita LiveOrderRouter solo con unlock y scope binance_demo.
  Transport local_demo_sim (fills en memoria). No producción ni testnet remoto
  hasta que el operador configure BINANCE_DEMO_* en env local. Bump 0.93.0.
  Fase: 101.

[2026-07-26] DEC-144 — LIVE gated por usuario/contraseña (corte humano)
  Decisión: el dueño autoriza desarrollar LIVE; el corte es unlock con
  QUANTLAB_LIVE_USER/PASSWORD locales (nunca en git). LIVE_BLOCKED=True sin
  unlock. Scope inicial binance_demo. MD público Binance read-only. Routing
  órdenes venue queda para fase siguiente. Bump 0.92.0.
  Fase: 100.

[2026-07-26] DEC-143 — Guided Lab MVP reutiliza lab scanner/backtest
  Decisión: F99 agrega wizard UI amigable (venue→scan→estrategia→simular) sin
  rutas nuevas: reutiliza POST /api/lab/scanner y /api/lab/backtest. Solo paper.
  LIVE_BLOCKED visible. Camino producto: Binance luego A3. Bump 0.91.0.
  Fase: 99.

[2026-07-26] DEC-142 — Freeze documental arco ops F93–F97 en v0.90
  Decisión: F98 congela docs del arco de visibilidad operativa (Venues, API
  Explorer, Diagnostics, Download, Support Bundle) en MILESTONE_V090_OPS_ARC_FREEZE.md.
  Sin cambios de runtime. Bump 0.90.0. LIVE_BLOCKED=True; sin FASE_98_APPROVED.
  Fase: 98.

[2026-07-26] DEC-141 — Support Bundle ZIP sin journal/book
  Decisión: F97 empaqueta snapshots read-only (diagnostics/about/openapi/venues/
  reconciliation) en un ZIP en memoria para reportes de incidente. Explícitamente
  excluye journal/book y credenciales. Reutiliza handlers F93–F96; sin mutaciones.
  Bump 0.89.0. LIVE_BLOCKED=True; sin FASE_97_APPROVED.
  Fase: 97.

[2026-07-26] DEC-140 — Descarga de diagnóstico reutiliza F95
  Decisión: F96 agrega GET /api/diagnostics.json que reutiliza el snapshot
  read-only de F95 y lo entrega como archivo adjunto (patrón fills.csv F65),
  con nombre saneado por session_id. Sin nuevas fuentes ni mutaciones.
  Bump 0.88.0. LIVE_BLOCKED=True; sin FASE_96_APPROVED.
  Fase: 96.

[2026-07-26] DEC-139 — /api/diagnostics agrega, no reemplaza
  Decisión: F95 agrega un snapshot read-only agregado (/api/diagnostics) que
  compone versión/modo/salud/conexión/reconciliación en un payload compacto para
  soporte, sin reemplazar /api/health ni exponer acciones. Pane con copiar JSON.
  Estrictamente read-only. Bump 0.87.0. LIVE_BLOCKED=True; sin FASE_95_APPROVED.
  Fase: 95.

[2026-07-26] DEC-138 — API Explorer reutiliza openapi.json
  Decisión: F94 agrega un visor de catálogo API en UI consumiendo el endpoint
  existente GET /api/openapi.json (F55), sin crear rutas nuevas ni permitir
  ejecutar requests desde el pane. Filtro en cliente; estrictamente read-only.
  Bump 0.86.0. LIVE_BLOCKED=True; sin FASE_94_APPROVED.
  Fase: 94.

[2026-07-26] DEC-137 — Venues panel sin rutas nuevas
  Decisión: F93 expone el registro de brokers en UI enriqueciendo el GET
  /api/venues existente (connected_venue, md_provider, mode, live_blocked,
  plugin_contract v1) en vez de crear una ruta nueva. Pane estrictamente
  read-only: única llamada QLApi.venues(); sin connect/disconnect ni plugins
  runtime. Bump 0.85.0. LIVE_BLOCKED=True; sin FASE_93_APPROVED.
  Fase: 93.

[2026-07-26] DEC-136 — Freeze documental del arco v0.71–v0.83
  Decisión: F92 congela docs del arco F79–F91 (el hito 0.80 quedó embebido:
  F88 tomó la versión 0.80.0). MILESTONE_V080_ARC_FREEZE.md + CHANGELOG sync
  0.81/0.82/0.83/0.84. Sin cambios de runtime. Bump 0.84.0. LIVE_BLOCKED=True;
  sin flip; sin FASE_92_APPROVED.
  Fase: 92.

[2026-07-26] DEC-135 — Rehydrate explícito post-rebuild; nunca auto-recovery
  Decisión: F91 agrega POST /api/paper/reconciliation/rehydrate que hace
  teardown del runtime y relee journal/book desde disco reusando
  switch_session. No reconstruye archivos ni auto-recupera: durable inválido
  queda bloqueado como al boot; broker queda desconectado y la reconexión es
  explícita. Evento 'rehydrate' en activity allowlist. Bump 0.83.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_91_APPROVED.
  Fase: 91.

[2026-07-26] DEC-134 — Panel Reconciliación Paper estrictamente read-only
  Decisión: F90 agrega UI del estado journal/book (F88) consumiendo solo
  GET /api/paper/reconciliation. La UI muestra rebuild_via (CLI offline) y
  no expone ningún camino HTTP mutable; test+smoke verifican ausencia de
  verbos mutadores en el fuente del panel. Bump 0.82.0. LIVE_BLOCKED=True;
  sin flip; sin FASE_90_APPROVED.
  Fase: 90.

[2026-07-26] DEC-133 — Certificación A3 MD read-only en lanes no sustituibles
  Decisión: F89 define fake-contract obligatorio CI/offline y sandbox-env opt-in
  únicamente simulation. Sandbox usa pyRofex con resolución strict sin fallback;
  SKIPPED_NOT_REQUESTED no equivale a PASS. El reporte excluye secretos, account
  IDs y payloads raw; place_order/cancel_order son write-bomb y PASS exige cero
  writes. Bump 0.81.0. LIVE_BLOCKED=True; sin flip; sin FASE_89_APPROVED.
  Fase: 89.

[2026-07-26] DEC-132 — Journal PAPER autoritativo; book como proyección
  Decisión: F88 define journal.jsonl append-only como source of truth y
  book.json v2 como proyección atómica reconstruible, ligada por checkpoint
  record_count/last_fill_id/SHA-256. Commit: preview, append+fsync, apply,
  persist atómico. Drift/corrupción bloquea submits. No auto-rebuild al boot
  ni rebuild HTTP; sólo CLI offline con backup. Bump 0.80.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_88_APPROVED.
  Fase: 88.

[2026-07-26] DEC-131 — Broker Plugin Contract v1 fail-closed
  Decisión: F87 versiona plugins externos con BrokerPluginSpec API "1";
  capabilities permitidas sólo market_data/account_read; registry aplica
  ReadOnlyBrokerPort siempre y bloquea submit/cancel; factory signature.bind
  antes de una única invocación, sin retry de TypeError interno; legacy v0
  emite warning; test kit cooperativo/offline no es sandbox. Bump 0.79.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_87_APPROVED.
  Fase: 87.

[2026-07-26] DEC-130 — Maximize / Restore Window
  Decisión: F86 = wm.js maximize/restoreFromMaximize/toggleMaximize;
  store preMax geometry; titlebar btn + dblclick; command palette
  action.maximize_window / action.restore_from_maximize; menú Inicio +
  context menu; layout.maximized bool + pre-max as x/y/w/h; bump 0.78.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_86_APPROVED.
  Fase: 86.

[2026-07-26] DEC-129 — Bring to Front / Send to Back (z-order)
  Decisión: F85 = wm.js bringToFront/sendToBack; command palette
  action.bring_to_front / action.send_to_back; menú Inicio + context
  menu titlebar; snapshotLayout ya persiste z; open/mergeOpts restauran z;
  bump 0.77.0. LIVE_BLOCKED=True; sin flip; sin FASE_85_APPROVED.
  Fase: 85.

[2026-07-26] DEC-128 — Cascade / Tile Windows
  Decisión: F84 = wm.js cascadeWindows/tileWindows; pure cascadeRects/
  tileRects + espejo Python window_layout; command palette
  action.cascade_windows / action.tile_windows; menú Inicio grupo
  Ventanas; scheduleSave persiste x/y/w/h; bump 0.76.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_84_APPROVED.
  Fase: 84.

[2026-07-26] DEC-127 — Minimize / Restore All windows
  Decisión: F83 = wm.js minimizeAll/restoreAll; command palette
  action.minimize_all / action.restore_all; menú Inicio grupo Ventanas;
  scheduleSave persiste minimized en layout.json; bump 0.75.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_83_APPROVED.
  Fase: 83.

[2026-07-26] DEC-126 — Window Snap to Edges
  Decisión: F82 = al soltar drag en wm.js, snapPosition(x,y,w,h,vw,vh,12)
  alinea a bordes/viewport si distancia < 12px; scheduleSave persiste;
  espejo Python snap_position para unit tests; bump 0.74.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_82_APPROVED.
  Fase: 82.

[2026-07-26] DEC-125 — Custom Preset Delete
  Decisión: F81 = DELETE /api/presets/{name} borra solo presets custom
  (session/presets/{name}.json); built-ins research|trading_paper|ops
  inmutables (400); UI × en lista custom; bump 0.73.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_81_APPROVED.
  Fase: 81.

[2026-07-26] DEC-124 — Custom Preset Save
  Decisión: F80 = POST /api/presets/save {name} guarda layout.json
  actual en session/presets/{name}.json; GET /api/presets incluye
  custom; apply funciona para custom; UI Guardar espacio actual;
  bump 0.72.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_80_APPROVED.
  Fase: 80.

[2026-07-26] DEC-123 — Watchlist Import/Export JSON
  Decisión: F79 = GET /api/watchlist/export (JSON download) +
  POST /api/watchlist/import {symbols, mode=merge|replace};
  botones Export/Import en panel Universe; bump 0.71.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_79_APPROVED.
  Fase: 79.

[2026-07-26] DEC-122 — Milestone Freeze Docs + CHANGELOG Sync (v0.70)
  Decisión: F78 = freeze documental hito v0.70.0 (MILESTONE_V070_FREEZE);
  inventario F19–F77/F78; sync CHANGELOG/RESUMEN/MEMORY/README;
  smoke version starts with 0.70; bundle default F19–F78; bump 0.70.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_78_APPROVED.
  Fase: 78.

[2026-07-26] DEC-121 — Broker Disconnect + Milestone prep
  Decisión: F77 = POST /api/broker/disconnect cierra broker y limpia
  estado conectado; conserva last_broker_connect para reconnect;
  botón Desconectar en Market + Health; bump 0.69.0; prep v0.70.
  LIVE_BLOCKED=True; sin flip; sin FASE_77_APPROVED.
  Fase: 77.

[2026-07-26] DEC-120 — Broker Reconnect Button
  Decisión: F76 = POST /api/broker/reconnect re-run last connect
  params desde session meta (last_broker_connect); connect persiste
  config; botón Reconectar en Market + Health; bump 0.68.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_76_APPROVED.
  Fase: 76.

[2026-07-26] DEC-119 — Broker Heartbeat Status
  Decisión: F75 = GET /api/broker/heartbeat llama broker.health()
  si hay broker conectado; si no, status=disconnected (HTTP 200);
  status bar muestra ok/fail; shell poll N=5s; bump 0.67.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_75_APPROVED.
  Fase: 75.

[2026-07-26] DEC-118 — Status Bar Clock Timezone
  Decisión: F74 = settings.timezone default "UTC" (opciones UTC|local);
  el reloj de la status bar respeta la preferencia (JS timeZone);
  bump 0.66.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_74_APPROVED.
  Fase: 74.

[2026-07-26] DEC-117 — Optional Sound Alerts
  Decisión: F73 = settings.sound_alerts default false (opt-in);
  cuando true, WebAudio beep corto en toast errors y kill engage;
  sin assets externos; graceful si AudioContext ausente; bump 0.65.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_73_APPROVED.
  Fase: 73.

[2026-07-26] DEC-116 — Desktop Notifications Hook
  Decisión: F72 = settings.desktop_notifications default false (opt-in);
  cuando true, JS Notification API en toast errors y kill engage;
  graceful si permiso denegado / API ausente; bump 0.64.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_72_APPROVED.
  Fase: 72.

[2026-07-26] DEC-115 — Health Extended + 1000 Tests Milestone
  Decisión: F71 = extender GET /api/health y /api/about con flags
  paper_kill_engaged, auto_backup_minutes, access_log; suite útil
  test_health_extended_f71.py (edge kill/pnl/equity/risk/backups/fills
  vacíos) para cruzar ≥1000 pytest passed; bump 0.63.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_71_APPROVED.
  Fase: 71.

[2026-07-26] DEC-114 — Paper Kill Switch
  Decisión: F70 = paper kill switch (meta.paper_kill_engaged);
  when engaged reject paper submit + session step with ValidationError;
  POST/GET /api/paper/kill; big red ENGAGE/DISENGAGE in Risk + Sesión Paper;
  suite test_paper_kill_f70.py; bump 0.62.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_70_APPROVED.
  Fase: 70.


[2026-07-26] DEC-113 — Risk Utilization Report
  Decisión: F69 = GET /api/risk/utilization (% used max_qty/notional
  vs PaperBook posiciones; peak qty + gross notional Decimal-safe);
  sección Utilización en panel Risk; suite test_risk_utilization_f69.py;
  bump 0.61.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_69_APPROVED.
  Fase: 69.

[2026-07-26] DEC-112 — Milestone Freeze Docs + CHANGELOG Sync (v0.60)
  Decisión: F68 = freeze documental del hito workbench v0.60.0:
  inventario F19–F67/F68, invariantes Zero-Trust, cómo operar research/paper,
  límites explícitos (no LIVE); sync CHANGELOG (resumen agrupado F19–F67),
  RESUMEN, PROJECT_MEMORY, README; smoke tip version starts with 0.60;
  bundle INTERNAL default F19–F68; bump 0.60.0. LIVE_BLOCKED=True; sin flip;
  sin FASE_68_APPROVED. Sin features de producto nuevas.
  Fase: 68.

[2026-07-26] DEC-111 — Paper PnL Summary
  Decisión: F67 = PaperBook.get_pnl (realized/unrealized/equity/cash)
  desde book + marks; GET /api/paper/pnl (marks PaperBroker mid/last
  o avg fallback); headers PnL en Positions y Blotter; suite
  test_paper_pnl_f67.py; bump 0.59.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_67_APPROVED.
  Fase: 67.

[2026-07-26] DEC-110 — Equity Curve Snapshot
  Decisión: F66 = equity.jsonl append-only (ts, equity, cash) al aplicar
  fills paper (on_book_change) y en cada paper session step (on_step);
  GET /api/paper/equity?limit=N; sección Equity curve en Positions con
  lista + sparkline SVG; suite test_equity_curve_f66.py; bump 0.58.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_66_APPROVED.
  Fase: 66.

[2026-07-26] DEC-109 — Blotter CSV Server Export
  Decisión: F65 = GET /api/paper/fills.csv (text/csv del PaperFillJournal)
  con Content-Disposition attachment; botones Descargar CSV en Blotter y
  Journal; fills_to_csv header estable alineado a F28; suite
  test_fills_csv_f65.py; bump 0.57.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_65_APPROVED.
  Fase: 65.

[2026-07-26] DEC-108 — Backups Panel UI
  Decisión: F64 = panel SPA Backups consumiendo GET /api/backups (F63) +
  POST /api/backups/run (trigger manual run_auto_backup); menú Inicio +
  command palette open.backups; botón Backup ahora; suite test_backups_ui_f64.py;
  bump 0.56.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_64_APPROVED.
  Fase: 64.

[2026-07-26] DEC-107 — Session Auto-Backup
  Decisión: F63 = auto-backup ZIP opcional vía settings.auto_backup_minutes
  (default 0=off); si >0 thread daemon exporta a session/backups/ con
  rotación max 5; GET /api/backups; reusa session_zip allowlist+zip-slip;
  run_auto_backup trigger manual para tests; bump 0.55.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_63_APPROVED.
  Fase: 63.

[2026-07-26] DEC-106 — Access Log Panel UI
  Decisión: F62 = panel SPA Access Log consumiendo GET /api/access-log (F61);
  menú Inicio + command palette open.access_log; auto-refresh opcional 5s;
  dispose al cerrar; suite test_access_log_ui_f62.py; bump 0.54.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_62_APPROVED.
  Fase: 62.

[2026-07-26] DEC-105 — Workbench Request Access Log
  Decisión: F61 = access.jsonl append-only por sesión (method, path, status, ms)
  sin bodies/secrets; toggle settings.access_log default true; GET /api/access-log;
  middleware server; suite test_access_log_f61.py; bump 0.53.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_61_APPROVED.
  Fase: 61.

[2026-07-26] DEC-104 — i18n Scaffold (es default, en stub)
  Decisión: F60 = scaffold i18n UI workbench: diccionario es (default) +
  stub en en static/js/i18n.js y static/i18n/*.json; QLi18n.t()/applyDom
  aplicado en shell al load desde settings.locale; GET /api/i18n/{locale};
  ALLOWED_LOCALES es|en; suite test_i18n_f60.py; bump 0.52.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_60_APPROVED. Sin cobertura i18n
  completa de paneles lab.
  Fase: 60.

[2026-07-26] DEC-103 — A11y Basics (focus + aria)
  Decisión: F59 = mejoras a11y mínimas en SPA estático workbench:
  role=dialog + aria-modal + aria-label en Command Palette / About /
  Onboarding (shells en index.html); aria-label en botones taskbar;
  focus trap básico Tab en palette; skip link «Ir al contenido»;
  suite test_a11y_f59.py; bump 0.51.0. LIVE_BLOCKED=True; sin flip;
  sin FASE_59_APPROVED. Sin axe-core / SR E2E completo.
  Fase: 59.

[2026-07-26] DEC-102 — Milestone Freeze Docs + CHANGELOG Sync (v0.50)
  Decisión: F58 = freeze documental del hito workbench v0.50.0:
  inventario F19–F57/F58, invariantes Zero-Trust, cómo operar research/paper,
  límites explícitos (no LIVE); sync CHANGELOG (resumen agrupado F19–F57),
  RESUMEN, PROJECT_MEMORY, README; smoke tip version starts with 0.50;
  bundle INTERNAL default F19–F58; bump 0.50.0. LIVE_BLOCKED=True; sin flip;
  sin FASE_58_APPROVED. Sin features de producto nuevas.
  Fase: 58.

[2026-07-26] DEC-101 — Content-Security-Policy
  Decisión: F57 = CSP restrictiva en respuestas del workbench compatible
  con SPA local: default-src 'self'; script-src 'self'; style-src 'self'
  'unsafe-inline' (atributos style= en panes JS); connect-src 'self';
  frame-ancestors 'none'; sin unsafe-eval; HTML sin scripts inline
  (todos externos /static/js); CONTENT_SECURITY_POLICY en
  quantlab.workbench.security_headers; suite test_csp_f57.py; bump 0.49.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_57_APPROVED.
  Fase: 57.

[2026-07-26] DEC-100 — Security Headers
  Decisión: F56 = headers de seguridad en respuestas del workbench
  (X-Content-Type-Options nosniff, X-Frame-Options DENY,
  Referrer-Policy no-referrer; Cache-Control no-store en /api/*);
  CORS fail-closed: nunca Access-Control-Allow-Origin *; si Origin
  presente y no loopback → no reflejar; módulo
  quantlab.workbench.security_headers; suite test_security_headers_f56.py;
  bump 0.48.0. LIVE_BLOCKED=True; sin flip; sin FASE_56_APPROVED.
  Fase: 56.

[2026-07-26] DEC-099 — OpenAPI / API Catalog
  Decisión: F55 = GET /api/openapi.json genera schema OpenAPI 3 mínimo
  desde catálogo estático quantlab.workbench.api_catalog (sin FastAPI);
  lista paths/methods/summary research-safe; sin rutas LIVE trading;
  link About → /api/openapi.json; suite test_openapi_f55.py; bump 0.47.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_55_APPROVED.
  Fase: 55.

[2026-07-26] DEC-098 — Readiness / Liveness Probes
  Decisión: F54 = GET /api/livez (siempre 200 si proceso up) +
  GET /api/readyz (200 si LIVE_BLOCKED True y session root writable; 503 si no);
  módulo quantlab.workbench.probes; documentado en DOCKER_WORKBENCH.md para
  Docker HEALTHCHECK; suite test_probes_f54.py; bump 0.46.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_54_APPROVED.
  Fase: 54.

[2026-07-26] DEC-097 — Dockerfile Workbench (opt-in)
  Decisión: F53 = Dockerfile.workbench opt-in (python:3.12-slim + uv sync);
  EXPOSE 8765; CMD quantlab-workbench --host 0.0.0.0 --allow-non-loopback
  --no-browser (riesgo documentado: solo para Docker Desktop port-map;
  publicar -p 127.0.0.1:8765:8765; sin auth HTTP; no WAN);
  .dockerignore excluye .env / data secrets; docs/ops/DOCKER_WORKBENCH.md;
  tests parsean Dockerfile (sin build obligatorio); bump 0.45.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_53_APPROVED.
  Fase: 53.

[2026-07-26] DEC-096 — Graceful Shutdown + Paper Session Safety
  Decisión: F52 = SIGINT/SIGTERM en launch.py detienen PaperSessionRunner,
  flushean layout.json/settings.json (+ book) y apagan el HTTPServer;
  POST /api/shutdown solo loopback marca flag y dispara el mismo hook
  (pensado para tests/automatización; camino normal = señales);
  módulo quantlab.workbench.shutdown; suite test_shutdown_f52.py;
  bump 0.44.0. LIVE_BLOCKED=True; sin flip; sin FASE_52_APPROVED.
  Fase: 52.

[2026-07-26] DEC-095 — API Rate Limit (loopback soft)
  Decisión: F51 = soft rate limit in-process del workbench HTTP (token bucket
  keyed por IP+path); default 120 req/s · burst 120 para no romper tests;
  429 JSON + Retry-After al exceder; inyección vía
  WorkbenchState.configure_rate_limit; suite test_rate_limit_f51.py;
  bump 0.43.0. LIVE_BLOCKED=True; sin flip; sin FASE_51_APPROVED.
  Fase: 51.

[2026-07-26] DEC-094 — Performance Baseline Workbench API
  Decisión: F50 = medir latencia loopback de endpoints clave (health, mode,
  commands, about, lab/capabilities) sobre ThreadingHTTPServer en thread;
  assert p95 y max < 500ms local; módulo quantlab.workbench.perf_baseline +
  tests test_perf_baseline_f50.py + script workbench_perf_baseline.py;
  bump 0.42.0. LIVE_BLOCKED=True; sin flip; sin FASE_50_APPROVED.
  Fase: 50.

[2026-07-26] DEC-093 — Milestone Freeze Docs + CHANGELOG Sync
  Decisión: F49 = freeze documental F19–F48 (MILESTONE_V040_FREEZE.md:
  inventario, invariantes, cómo operar, límites no LIVE); sync CHANGELOG
  con resumen agrupado F19–F48 + RESUMEN/PROJECT_MEMORY/README; smoke
  «about version matches __version__»; bundle default F19–F49; bump 0.41.0.
  LIVE_BLOCKED=True; sin flip; sin FASE_49_APPROVED.
  Fase: 49.

[2026-07-26] DEC-092 — Theme CSS Completion (slate + high-contrast)
  Decisión: F48 = completar tokens CSS para themes slate | high-contrast
  (chrome: banner/status/taskbar/desktop/overlays); data-theme en
  documentElement + body al load settings y al PUT /api/settings;
  tests test_themes_f48.py + smoke theme roundtrip. LIVE_BLOCKED=True;
  sin flip; sin FASE_48_APPROVED.
  Fase: 48.

[2026-07-26] DEC-091 — Chat Context Awareness
  Decisión: F47 = extender ToolRegistry allowlist read-only con
  get_session_summary (mode/venue/book equity/positions/activity last N),
  list_reports (list_lab_reports sesión), list_strategies (strategy_catalog);
  FakeProvider intents ES («cómo estoy», «resumen sesión», «qué reportes hay»,
  «estrategias»); tests test_chat_context_f47.py. LIVE_BLOCKED=True; sin flip;
  sin FASE_47_APPROVED; chat sin trading tools.
  Fase: 47.

[2026-07-26] DEC-090 — Multi-Session Switcher
  Decisión: F46 = GET /api/sessions (lista dirs bajo session root) +
  POST /api/sessions/switch {session_id} fail-closed validate_session_id
  (recrea journal/book/labs paths; detiene paper runner; limpia broker) +
  POST /api/sessions/new (crea + switch) + UI panel Sessions (Inicio /
  open.sessions); tests test_sessions_f46.py. LIVE_BLOCKED=True; sin flip;
  sin FASE_46_APPROVED; sin LIVE.
  Fase: 46.

[2026-07-26] DEC-089 — About Dialog + Version Badge
  Decisión: F45 = GET /api/about (version, live_blocked, phases_summary
  F19–F45 INTERNAL, python_version, bind_policy) + badge versión en
  status bar + diálogo Acerca de (menú Inicio); command open.about;
  tests test_about_f45.py. LIVE_BLOCKED=True; sin flip;
  sin FASE_45_APPROVED; sin LIVE.
  Fase: 45.

[2026-07-26] DEC-088 — E2E Paper Workflow Integration Test
  Decisión: F44 = test de integración (sin browser) que ejercita el
  flujo paper completo vía API workbench en thread loopback:
  mode paper → connect binance/a3 tester → submit → positions/book →
  paper session buy_once+step → backtest+reports → validation +
  optimize + mc (mini) → export HB → session zip → LIVE reject;
  tests test_e2e_paper_workflow_f44.py. LIVE_BLOCKED=True; sin flip;
  sin FASE_44_APPROVED; sin LIVE; sin browser E2E.
  Fase: 44.

[2026-07-26] DEC-087 — Red-team Workbench Hardening
  Decisión: F43 = auditoría red-team APIs workbench + remediación
  fail-closed: zip_path sandbox (session parent), create_server
  requiere allow_non_loopback fuera de loopback, body JSON default
  2 MiB, csv_path anti-traversal, path segments anti-..;
  tests test_redteam_f43.py. LIVE_BLOCKED=True; sin flip;
  sin FASE_43_APPROVED; sin LIVE.
  Fase: 43.

[2026-07-26] DEC-086 — Ops Metrics Panel
  Decisión: F42 = exponer quantlab.infra.ops_metrics en workbench;
  GET /api/ops/metrics (JSON counters) + GET /api/ops/prometheus
  (text/plain); UI panel tabla counters con highlight
  live_gate.blocked; sin flip LIVE; sin FASE_42_APPROVED.
  Fase: 42.

[2026-07-26] DEC-085 — Activity Log + Toasts
  Decisión: F41 = activity.jsonl append-only por sesión (events:
  connect, submit, backtest, optimize, export, error);
  GET /api/activity?limit=100; hooks mínimos en handlers clave;
  UI toasts success/error + panel Activity. LIVE_BLOCKED=True;
  sin flip; sin FASE_41_APPROVED; sin LIVE.
  Fase: 41.

[2026-07-26] DEC-084 — Workspace Presets
  Decisión: F40 = presets built-in research / trading_paper / ops;
  GET /api/presets lista; POST /api/presets/apply {name} reemplaza
  layout.json con ventanas del preset; UI menú Inicio → Espacios de
  trabajo. LIVE_BLOCKED=True; sin flip; sin FASE_40_APPROVED; sin LIVE.
  Fase: 40.

[2026-07-26] DEC-083 — Session Export/Import ZIP
  Decisión: F39 = export sesión durable a ZIP research-safe (journal,
  book, layout, settings, reports, optimizer, etc.) sin secretos;
  GET /api/session/export (JSON path o ?download=1 ZIP); POST
  /api/session/import (mode=new|merge fail-closed) con zip-slip via
  scale.backup._assert_safe_zip_member + allowlist; UI en Settings.
  LIVE_BLOCKED=True; sin flip; sin FASE_39_APPROVED; sin LIVE.
  Fase: 39.

[2026-07-26] DEC-082 — Docs / Help Browser
  Decisión: F38 = browser Help/Docs read-only sobre docs/*.md y
  docs/ops/*.md; GET /api/docs lista paths relativos safe; GET
  /api/docs/content?path= lee markdown con path traversal fail-closed;
  panel buscar + preview HTML simple (escape) o pre text; chat
  search_docs reutiliza el mismo listado (incluye ops/).
  LIVE_BLOCKED=True; sin flip; sin place_order venue.
  Fase: 38.

[2026-07-26] DEC-081 — First-run Onboarding Wizard
  Decisión: F37 = wizard modal first-run si session meta no tiene
  onboarding_done; 4 pasos (TESTER/REAL/LIVE bloqueado → conectar venue
  tester → Sesión Paper/Backtest → Chat IA safe); GET /api/onboarding +
  POST /api/onboarding/complete persisten flag en meta.json.
  LIVE_BLOCKED=True; sin flip; sin place_order venue.
  Fase: 37.

[2026-07-26] DEC-080 — Settings + Status Bar
  Decisión: F36 = settings.json por sesión (theme slate|high-contrast,
  default_venue, default_strategy, slippage_bps, locale=es);
  GET/PUT /api/settings; panel Settings UI; status bar fija inferior
  (mode, live_blocked, session_id, venue, md_provider, clock).
  LIVE_BLOCKED=True; sin flip.
  Fase: 36.

[2026-07-26] DEC-079 — Command Palette + Keyboard Shortcuts
  Decisión: F35 = command palette Windows-like (Ctrl+K / Ctrl+Shift+P)
  con GET /api/commands (paneles + acciones seguras: health refresh,
  close focused); atajos Ctrl+1..9 abren paneles; Esc cierra palette;
  Ctrl+W cierra ventana enfocada. Sin acciones LIVE / place_order /
  flip. LIVE_BLOCKED=True; sin flip.
  Fase: 35.

[2026-07-26] DEC-078 — Monte Carlo History + Hummingbot Export Wizard
  Decisión: F34 = POST /api/lab/montecarlo persiste summary en
  session/montecarlo/<run_id>/ (CI 95% mean/std/ci_low/ci_high);
  GET /api/lab/montecarlo/history lista/latest (+ GET /{run_id});
  panel MC enriquecido. Export HB wizard: lista experiments, pasos
  validate→build→export path-safe a session/exports (snapshot único +
  alias latest); GET /api/lab/exports lista previos; banner
  live_routing:false. LIVE_BLOCKED=True; sin flip; sin venue submit;
  sin path externo.
  Fase: 34.

[2026-07-26] DEC-077 — Optimizer History + Pareto Panel
  Decisión: F33 = POST /api/lab/optimize corre grid mini (lookback×qty →
  sharpe) y, con ≥2 trials, calcula frente Pareto sharpe↑/MDD↓ vía
  pareto_from_trials; persiste summary en session/optimizer/<run_id>/;
  GET /api/lab/optimize/history lista/latest (+ GET /{run_id}); panel
  Optimizer enriquecido (tabla + Pareto texto/JSON + SVG simple).
  LIVE_BLOCKED=True; sin flip; sin venue submit; sin path externo.
  Fase: 33.

[2026-07-26] DEC-076 — Validation / Walk-Forward Runner UI
  Decisión: F32 = POST /api/lab/validation/run genera train/val/OOS +
  walk-forward sobre barras sintéticas; reporta índices de segmentos +
  anti-leakage (check_temporal_leakage) por pares y folds; persiste
  summary en session/validation/<run_id>/; GET lista/latest (+ preview
  efímero si vacío); panel Validation enriquecido. LIVE_BLOCKED=True;
  sin flip; sin venue submit; sin path externo.
  Fase: 32.

[2026-07-26] DEC-075 — Feature Store Browser + Pipeline Runner UI
  Decisión: F31 = browser read-only FeatureStore (session/features o
  data/features / QUANTLAB_FEATURE_STORE_PATH); GET /api/lab/features/store;
  POST /api/lab/features/run (alias /api/lab/features) persiste demo
  close+simple_return+log_return vía FeatureStore.put en sandbox sesión;
  panel Features lista artifacts + columnas. LIVE_BLOCKED=True; sin flip;
  sin venue submit; sin path externo de store.
  Fase: 31.

[2026-07-26] DEC-074 — Universe Watchlist + Data Catalog Browser
  Decisión: F30 = watchlist.json por sesión (GET/PUT /api/watchlist add/remove);
  GET /api/universe (broker ∪ watchlist); GET /api/catalog read-only vía
  quantlab.data.catalog (SQLite/DuckDB; empty-ok si no hay archivo);
  paneles Universe (click → set symbol Market/Session) + Catalog UI mínimo.
  LIVE_BLOCKED=True; sin flip; sin venue submit; sin upsert catalog.
  Fase: 30.

[2026-07-26] DEC-073 — Lab Report Viewer + Metrics History
  Decisión: F29 = tras backtest lab exitoso persistir MetricsResult/summary
  JSON (+ HTML via ReportGenerator) en session reports/; API GET
  /api/lab/reports y GET /api/lab/reports/{id}; panel UI Reports
  (lista + preview HTML|JSON). POST implícito en /api/lab/backtest.
  LIVE_BLOCKED=True; sin flip; sin venue submit.
  Fase: 29.

[2026-07-26] DEC-072 — Workbench layout persistence + Journal viewer
  Decisión: F28 = layout.json por sesión (save/load geometría MDI);
  API GET/PUT /api/layout; wm.js debounce save en move/resize + restore
  al boot; panel Journal (tabla fills + export CSV client-side sobre
  GET /api/paper/fills). LIVE_BLOCKED=True; sin flip; sin venue submit.
  Fase: 28.

[2026-07-26] DEC-071 — Strategy Catalog workbench (paper + lab)
  Decisión: F27 = catálogo único `strategy_catalog` con metadata
  (id/nombre/defaults/tags mm|momentum); factory compartida para
  PaperSessionRunner + lab backtest; wire InventoryMM + Avellaneda–Stoikov;
  GET /api/lab/strategies; UI params básicos; MM en bar-backtest vía
  BarSyntheticBookAdapter (bid/ask sintéticos desde close). LIVE_BLOCKED=True.
  Fase: 27.

[2026-07-26] DEC-070 — Paper Session Runner (solo PaperBroker)
  Decisión: F26 = PaperSessionRunner start/stop/step (+ interval_ms daemon
  cancelable); estrategias research dummy/buy_once/momentum; risk en cada
  PLACE; submit solo PaperBroker (isinstance fail-closed); API
  /api/paper/session/* + panel UI. Nunca place_order venue; LIVE_BLOCKED=True.
  Fase: 26.

[2026-07-26] DEC-069 — Ops Desk 1-click + hardening loopback / experiment_id / slip
  Decisión: F25 = launcher scripts/launch_workbench.sh + .desktop;
  host no-loopback exige --allow-non-loopback (exit 2 + warning);
  experiment_id charset ^[A-Za-z0-9_-]+$; PaperBroker slippage_bps adverso;
  panel Riesgo GET /api/risk + banner session_id. LIVE_BLOCKED=True; sin Electron/auth WAN.
  Fase: 25.

[2026-07-26] DEC-068 — A3 MD read-only opt-in (env) + generics CSV/REST
  Decisión: md_source fake|env; env requiere QUANTLAB_A3_MD_READONLY=1 +
  QUANTLAB_A3_*; fallback fake; builtins generic_csv / generic_rest MD-only;
  workbench reporta md_provider. Órdenes venue siguen gated.
  Fase: 24.

[2026-07-26] DEC-067 — Entry points quantlab.brokers para venues externos
  Decisión: plugins vía importlib.metadata group quantlab.brokers;
  load_entry_point_brokers no crashea (warning); get_default_registry carga
  builtins + plugins. Ver docs/ops/BROKER_PLUGINS.md.
  Fase: 24.

[2026-07-26] DEC-066 — PaperBook + sesión durable + risk paper
  Decisión: F23 = PaperBook (cash/posiciones/avg/MTM, short fail-closed),
  WorkbenchSession bajo data/runtime/workbench/<id>/, PaperRiskLimits en
  paper submit, API positions/book/session, UI Posiciones; LIVE_BLOCKED=True.
  Fase: 23.

================================================================================
QUANTLAB — REGISTRO DE DECISIONES TÉCNICAS
================================================================================
Formato: [FECHA] DEC-XXX — Título — Decisión — Justificación
================================================================================

[2026-07-23] DEC-001 — Lenguaje principal
  Decisión: Python 3.11+
  Justificación: Ecosistema cuantitativo maduro, productividad, tipado gradual.
  Alternativas descartadas: Rust (prematura), Julia (ecosistema menor).

[2026-07-23] DEC-002 — Formato de almacenamiento
  Decisión: Apache Parquet
  Justificación: Columnar, comprimido, estándar, compatible con Polars/DuckDB.

[2026-07-23] DEC-003 — Motor analítico / catálogo
  Decisión: DuckDB (primario), SQLite (fallback)
  Justificación: Consulta Parquet nativa, analytics columnar, zero deps.

[2026-07-23] DEC-004 — Librería de transformación de datos
  Decisión: Polars (primario), Pandas (compatibilidad en fronteras)
  Justificación: 5-30x más rápido, lazy evaluation, menor consumo de RAM.

[2026-07-23] DEC-005 — Framework de tests
  Decisión: pytest
  Justificación: Estándar de facto, fixtures, parametrización, plugins.

[2026-07-23] DEC-006 — Logging
  Decisión: structlog sobre stdlib logging
  Justificación: Logs estructurados JSON, context binding.

[2026-07-23] DEC-007 — Configuración
  Decisión: YAML para runtime, TOML para proyecto (pyproject.toml)
  Justificación: Legible, versionable, schemas de validación.

[2026-07-23] DEC-008 — Separación investigación / ejecución
  Decisión: QuantLab investiga; Hummingbot ejecuta. ExecutionEngine v1 solo export.
  Justificación: Evita órdenes accidentales; frontera clara.

[2026-07-23] DEC-009 — Arquitectura por capas
  Decisión: 9 módulos con contratos en core/
  Justificación: Capas evolucionan independientemente.

[2026-07-23] DEC-010 — Licencia
  Decisión: MIT
  Justificación: Permisiva, estándar.

[2026-07-23] DEC-011 — Datos fuera de git
  Decisión: data/ y reports/ en .gitignore con .gitkeep
  Justificación: Datos pesados no pertenecen al repositorio.

[2026-07-23] DEC-012 — Progressive complexity
  Decisión: Empezar local/secuencial; escalar infra sin cambiar contratos de dominio.
  Justificación: Evita over-engineering.

[2026-07-23] DEC-013 — Disciplina de abstracción
  Decisión: No crear abstracción sin frontera externa real, dos implementaciones
  plausibles o necesidad demostrada.
  Justificación: Revisor técnico PROMPT 001.1; evita interfaces prematuras en Fase 2.
  Referencia: docs/Arquitectura.md §1.4, §6.

[2026-07-23] DEC-014 — Strategy event-driven con intenciones
  Decisión: Strategy.on_event() como contrato universal; produce OrderIntent
  (place/cancel/replace/no_action); on_bar() solo como adaptador bar-based.
  Justificación: on_bar/on_fill insuficiente para market making; revisor 001.1 §1.
  Referencia: docs/Arquitectura.md §6.1.

[2026-07-23] DEC-015 — Desacoplamiento metrics/reporting
  Decisión: metrics y reporting consumen SimulationResult, ExperimentManifest,
  MetricsResult desde core. No dependen de simulation ni research.
  Justificación: Revisor 001.1 §4; evita acoplamiento oculto.
  Referencia: docs/Diagrama.md §3.

[2026-07-23] DEC-016 — Backtester descompuesto
  Decisión: 7 componentes (Orchestrator, Market Replay, Clock, Strategy Runner,
  Execution Simulator, Ledger, Result Recorder) + 4 políticas intercambiables.
  Justificación: Revisor 001.1 §3; separación de responsabilidades.
  Referencia: docs/Arquitectura.md §7.

[2026-07-23] DEC-017 — Dos niveles de backtesting
  Decisión: Fase 5A bar-based (OHLCV) y Fase 5B microestructura (trades/book).
  MM no validable solo con OHLCV.
  Justificación: Revisor 001.1 §7-8.
  Referencia: docs/Arquitectura.md §7.4, §9.

[2026-07-23] DEC-018 — Slippage fixed bps solo bar-based
  Decisión: Fixed bps aceptable como baseline en 5A; NO default válido para MM ni 5B.
  Justificación: Revisor 001.1 §8.
  Referencia: docs/Arquitectura.md §9.3.

[2026-07-23] DEC-019 — Raw inmutable / processed derivado
  Decisión: data/raw/ nunca se muta; normalización solo en data/processed/.
  Justificación: Revisor 001.1 §11; trazabilidad y regeneración.
  Referencia: docs/Arquitectura.md §8.1.

[2026-07-23] DEC-020 — Scientific Validation antes de Optimizer
  Decisión: Fase 10 obligatoria (train/val/OOS, walk-forward, anti-leakage)
  antes de Fase 12 Optimizer.
  Justificación: Revisor 001.1 §9; walk-forward no puede quedar como FI.
  Referencia: docs/Arquitectura.md §13 Fase 10.

[2026-07-23] DEC-021 — Fase 2 acotada
  Decisión: Fase 2 implementa tipos, manifests, config, logging, CI — NO las 10 ABC.
  Justificación: Revisor 001.1 §5-6.
  Referencia: docs/Arquitectura.md §13 Fase 2.

[2026-07-23] DEC-022 — Reproducibilidad realista
  Decisión: Determinismo exacto en entorno controlado; equivalencia numérica con
  tolerancias documentadas entre entornos; versionado completo del entorno.
  Reemplaza "bit a bit en cualquier entorno".
  Justificación: Revisor 001.1 §13.
  Referencia: docs/Arquitectura.md §1.2 O2.

[2026-07-23] DEC-023 — Escalabilidad realista
  Decisión: Escalabilidad progresiva hacia 1M+ simulaciones sin modificar
  contratos principales del dominio; cambios permitidos en infra/orquestación.
  Justificación: Revisor 001.1 §13.
  Referencia: docs/Arquitectura.md §1.2 O3.

[2026-07-23] DEC-024 — ExecutionEngine v1 export-only
  Decisión: v1 limitado a validate_export, build_execution_package,
  export_configuration. Deploy/status/stop/kill switch = futuro.
  Justificación: Revisor 001.1 §12.
  Referencia: docs/Arquitectura.md §4.9, §6.9.

[2026-07-23] DEC-025 — CI desde Fase 2
  Decisión: CI básico en Fase 2: install limpio, lint, mypy, pytest, config validation.
  Justificación: Revisor 001.1 §6.
  Referencia: docs/Arquitectura.md §13 Fase 2.

[2026-07-23] DEC-026 — ExperimentManifest mínimo en Fase 2
  Decisión: ExperimentManifest (experiment_id, dataset, config, seed, commit,
  python/deps, platform, strategy/model versions, artifacts) desde Fase 2.
  Justificación: Revisor 001.1 §6; reproducibilidad temprana.
  Referencia: docs/Arquitectura.md §5.18.

[2026-07-23] DEC-027 — Tipos inmutables
  Decisión: @dataclass(frozen=True, slots=True) para tipos de dominio.
  Justificación: Inmutabilidad, mypy strict, alineación arquitectura §5.
  Fase: 2.

[2026-07-23] DEC-028 — StrEnum para enumeraciones
  Decisión: enum.StrEnum en lugar de (str, Enum).
  Justificación: Python 3.11+, ruff UP042, serialización natural.
  Fase: 2.

[2026-07-23] DEC-029 — Config Pydantic v2
  Decisión: AppConfig con Pydantic; campo json_output (no json).
  Justificación: Validación en frontera infra; evita colisión BaseModel.json.
  Fase: 2.

[2026-07-23] DEC-030 — Strategy como Protocol
  Decisión: typing.Protocol + @runtime_checkable; no ABC vacío.
  Justificación: Contrato aprobado DEC-014; cumple DEC-013.
  Fase: 2.

[2026-07-23] DEC-031 — DummyStrategy en research/strategies
  Decisión: Única implementación Strategy en Fase 2 para vertical slice.
  Fase: 2.

[2026-07-23] DEC-032 — Módulo vertical_slice + CLI
  Decisión: run_vertical_slice() + entry point quantlab-vertical-slice.
  Fase: 2.

[2026-07-23] DEC-033 — CI mínimo Fase 2
  Decisión: GitHub Actions: ruff, mypy strict, pytest cov≥80%, smoke tests.
  Fase: 2.

[2026-07-23] DEC-034 — Execution models placeholder Fase 2
  Decisión: execution_model_versions = none-phase2 en vertical slice.
  Justificación: Simulación no existe aún; campo presente para contrato.
  Fase: 2.

[2026-07-23] DEC-035 — Review Package obligatorio (PROMPT 003)
  Decisión: Al cierre de cada fase generar QuantLab_Review_Fase_XX_vY.Y.zip
  con código, tests, config, docs, reportes de calidad y PROJECT_STATUS.md.
  Script: scripts/build_review_package.py
  Justificación: Auditoría GPT sin solicitar archivos adicionales.
  Entregables: ZIP + REVIEW_REQUEST.md únicamente.

[2026-07-24] DEC-036 — Versionado explícito de schemas de manifests
  Contexto: Fase 3 persistirá datasets; sin política de schema_version el
  ecosistema romperá lecturas entre versiones.
  Problema: Confundir versión de schema, dataset, experimento y QuantLab.
  Alternativas:
    A) Sin versión hasta Fase 3 (rechazada: deuda silenciosa)
    B) schema_version semver + política documentada (elegida)
    C) Framework completo de migraciones ya (rechazada: prematuro)
  Decisión: Todo manifest exige schema_version; compatibilidad patch/minor/major
  documentada en docs/MANIFEST_VERSIONING.md; migraciones futuras explícitas.
  Consecuencias: Lectores rechazan major desconocida / sin versión; serialización
  canónica determinista; manifests históricos inmutables.
  Compatibilidad: schema 1.0 actual en DatasetManifest.
  Impacto futuro: datasets/experimentos versionados sin reescritura silenciosa.
  Fase: 2 (cierre v1.2).

[2026-07-24] DEC-037 — Review Package con política centralizada y validación ZIP
  Decisión: scripts/review_package_policy.py como fuente única; generación vía
  zipfile; validación pre/post; REVIEW_PACKAGE_MANIFEST.json; SHA-256 sidecar.
  Justificación: v1.1 filtró mal caches; auditoría exige cero elementos prohibidos.
  Fase: 2.

[2026-07-24] DEC-039 — Métricas autoritativas vía JUnit/coverage XML + ZIP en dos pasadas
  Contexto: v1.2 rechazado — test_count=0 en PROJECT_STATUS; reporte incluido solo prevalidación.
  Problema: parsing de consola y dependencia circular del SHA dentro del ZIP.
  Alternativas:
    A) Seguir con collect-only (rechazada)
    B) pytest-json-report plugin extra (viable, más deps)
    C) JUnit XML stdlib + QualitySummary + ZIP 2 pasadas (elegida)
  Decisión: QualitySummary tipado; conteo desde JUnit; cobertura desde coverage.xml;
  validación estructural dentro del ZIP; SHA-256 solo en sidecar + .validation.txt externo.
  Fase: 2 (v1.3).

[2026-07-24] DEC-040 — Adaptador anticorrupción A3
  Decisión: Dominio no importa pyRofex; DTOs+mappers en data/exchanges/a3.
  Fase: 3.

[2026-07-24] DEC-041 — Separación Data Plane / Execution Plane
  Decisión: Market data operable sin habilitar órdenes; execution gated.
  Fase: 3.

[2026-07-24] DEC-042 — Producción bloqueada por múltiples gates
  Decisión: environment+allow_live+env confirm+allowlists+risk+kill switch.
  Fase: 3.

[2026-07-24] DEC-043 — Barras OHLCV desde trades
  Decisión: get_trade_history → agregación determinista versionada (schema 1.0).
  Fase: 3.

[2026-07-24] DEC-044 — Raw append-only
  Decisión: payloads externos nunca se sobrescriben; checksum + ingestion_run_id.
  Fase: 3.

[2026-07-24] DEC-045 — Fill bar-based inmediato (Fase 4 MVP)
  Decisión: ImmediateBarFillModel en la misma barra (MARKET@close; LIMIT por touch).
  No microestructura 5B; reproducible y testeable offline.
  Fase: 4.

[2026-07-24] DEC-046 — Métricas versionadas MetricsEngine 1.0.0
  Decisión: Sharpe/MDD/WR/PF desde SimulationResult → MetricsResult inmutable.
  Fase: 4.

[2026-07-24] DEC-047 — Alpha Scanner scoring min-max ponderado
  Decisión: volatilidad + volumen + liquidez (1/rango); ranking determinista.
  Fase: 4.

[2026-07-24] DEC-048 — Políticas de ejecución en paquete `execution/`
  Decisión: SlippageModel/LatencyModel fuera de `core/`; defaults NoSlippage +
  ZeroLatency preservan comportamiento Fase 4; inyección opcional en BarSimulationEngine.
  Fase: 5 (Módulo 1).

[2026-07-24] DEC-049 — FeeModel maker/taker con Decimal
  Decisión: ZeroFee / Proportional / MakerTakerFeeModel; motor usa FeeAssessment;
  SimulationConfig.fee_rate mapea a ProportionalFeeModel si > 0.
  Fase: 5 (Módulo 2).

[2026-07-24] DEC-051 — Capa `quantlab.features` (F5 Oficial M1)
  Decisión: contratos FeatureTransformer/Indicator + transformers causales;
  features→core permitido; core↛features; anti-lookahead por prefijo estable.
  Fase: 5 Oficial (Features).

[2026-07-24] DEC-052 — FeaturePipeline composable (F5 Oficial M2)
  Decisión: FeatureFrame inmutable; steps vectoriales; then() sin mutar.
  Fase: 5 Oficial (Features).

[2026-07-24] DEC-053 — FeatureStore + indicadores básicos (F5 Oficial M3)
  Decisión: store JSON versionado+checksum; SMA/EMA/RSI/ATR causales Decimal.
  Fase: 5 Oficial (Features).

[2026-07-26] DEC-054 — OperatingMode TESTER / PAPER / LIVE
  Decisión: tres modos formales en quantlab.brokers.mode; default_mode()=TESTER.
  ModeGuard.validate_boot fail-closed si LIVE y LIVE_BLOCKED.
  Fase: 19.

[2026-07-26] DEC-055 — Alias producto REAL = PAPER
  Decisión: REAL_ALIAS = OperatingMode.PAPER; resolve_mode("real")→PAPER.
  REAL = MD/cuenta reales + fills simulados. REAL ≠ LIVE.
  Fase: 19.

[2026-07-26] DEC-056 — BrokerPort multiplataforma + registry
  Decisión: Protocol BrokerPort + DTOs neutrales; BrokerRegistry con factories
  built-in a3 / binance / paper. create() siempre ModeGuard.validate_boot.
  Fase: 19.

[2026-07-26] DEC-057 — PaperBroker fills sin envío venue
  Decisión: PaperBroker envuelve md_port; PLACE→fill mid local + journal;
  NUNCA llama submit/cancel del venue subyacente (aunque venue_id sugiera live).
  Fase: 19.

[2026-07-26] DEC-058 — PaperFillJournal ≠ LocalPaperLedger
  Decisión: journal JSONL append-only source=paper_broker para fills PAPER;
  LocalPaperLedger SQLite sigue siendo auditoría de sims research. No mezclar.
  Fase: 19.

[2026-07-26] DEC-059 — A3BrokerPort MD-only execution plane
  Decisión: A3BrokerPort mapea FakeA3Backend→Broker*; submit/cancel SIEMPRE
  assert_live_routing_blocked() (también en PAPER). PAPER usa PaperBroker wrapper.
  Fase: 19.

[2026-07-26] DEC-060 — LIVE flip checklist; flip NO ejecutado
  Decisión: docs/ops/LIVE_FLIP_CHECKLIST.md; LIVE_BLOCKED permanece True.
  Flip solo con Meta-Auditor + dueño + commit dedicado. F19 no lo ejecuta.
  Fase: 19.

[2026-07-26] DEC-061 — Workbench local stdlib + SPA window-manager
  Decisión: F20 = ThreadingHTTPServer + static SPA (sin deps UI nuevas);
  bind default 127.0.0.1; entry quantlab-workbench; OperatingMode.LIVE rechazado;
  connect siempre PaperBroker + journal sesión; LIVE_BLOCKED permanece True;
  sin chat (F22) ni paneles F21; sin flip LIVE.
  Fase: 20.

[2026-07-26] DEC-062 — Lab panels research-safe sobre workbench
  Decisión: F21 = lab_services thin adapters + /api/lab/* + paneles SPA;
  demos sintéticos / tmp sesión; export HB path-safe con live_routing false;
  LIVE_BLOCKED permanece True; sin chat (F22); sin flip LIVE; sin credenciales.
  Fase: 21.

[2026-07-26] DEC-063 — Chat IA safe-by-default en workbench
  Decisión: F22 = ChatOrchestrator + ToolRegistry allowlist solo lectura/
  explicación; PROHIBIDO submit/cancel/place_order/set_live/flip_live_blocked;
  API POST /api/chat + GET /api/chat/tools; UI panel Chat + banner
  "Asistente research — no envía órdenes"; LIVE_BLOCKED permanece True.
  Fase: 22.

[2026-07-26] DEC-064 — FakeProvider default; LLM opt-in vía env
  Decisión: FakeProvider determinista (ES intents) es default CI/offline.
  OptionalEnvProvider solo si QUANTLAB_LLM_API_KEY != DISABLED; placeholders
  en .env.example sin secretos; tests CI no usan OptionalEnvProvider real.
  Fase: 22.

[2026-07-26] DEC-065 — chat_audit.jsonl append-only
  Decisión: cada turno de chat se audita en JSONL append-only (tmp sesión);
  incluye message, tools_used, mode, live_blocked; sin rewrite/delete.
  Fase: 22.

================================================================================
